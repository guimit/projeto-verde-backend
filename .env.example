DATABASE_URL=postgresql://user:password@localhost:5432/projeto_verde
JWT_SECRET=change_me_in_production
PORT=3001
FRONTEND_URL=http://localhost:3000

# Seed do primeiro platform_admin (npm run db:seed)
ADMIN_EMAIL=contato@guilhermemenezes.com
ADMIN_PASSWORD=changeme123

# Bird.com
BIRD_API_KEY=
BIRD_WORKSPACE_ID=
# Segredo partilhado para validar o webhook de inbound (header x-webhook-secret).
# Se vazio, o endpoint /api/webhooks/bird aceita qualquer pedido.
BIRD_WEBHOOK_SECRET=
# Palavras (separadas por vírgula) que contam como opt-in numa mensagem recebida.
OPTIN_KEYWORDS=sim,quero,aceito,yes,start
